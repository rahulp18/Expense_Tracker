import { makeStyles } from "@material-ui/core";
export default makeStyles((theme)=>({
    radioGroup: {
        display: 'flex',
        justifyContent: 'center',
        marginBottom: '-10px',
      },
      button: {
        marginTop: '20px',
      },

}));